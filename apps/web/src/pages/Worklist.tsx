import {FormEvent,useMemo,useState} from 'react'
import {useMutation,useQuery} from '@tanstack/react-query'
import {CheckCircle2,ListChecks,Search,UploadCloud} from 'lucide-react'
import {Link} from 'react-router-dom'
import {api,DicomElement,Endpoint,Result,WorklistEntry} from '../api/client'
import {TargetSelector} from '../components/dicom/TargetSelector'
import {PageHeader} from '../components/layout/PageHeader'
import {Button} from '../components/ui/Button'
import {SectionCard} from '../components/ui/Card'
import {Column,DataTable} from '../components/ui/DataTable'
import {CollapsiblePanel,CopyButton} from '../components/ui/Disclosure'
import {AlertBox} from '../components/ui/Feedback'
import {DateInput,FormField,Select,TextInput} from '../components/ui/Form'
import {Modal} from '../components/ui/Overlay'
import {StatusBadge} from '../components/ui/Status'
import {SaveTargetButton} from '../components/SaveTargetButton'
import {loadLocalSettings} from '../settings/localSettings'
import {localDateString} from '../utils/dates'

function DicomTree({items,depth=0}:{items:DicomElement[];depth?:number}){return <div className="dicom-tree">{items.map((item,index)=><div className="dicom-element" style={{paddingLeft:depth*15}} key={`${item.tag}-${index}`}><span>{item.tag}</span><b>{item.name}</b>{Array.isArray(item.value)?item.value.map((children,i)=><DicomTree key={i} items={children} depth={depth+1}/>):<code>{item.value||'—'}</code>}</div>)}</div>}
export function Worklist(){const {data:targets=[]}=useQuery({queryKey:['targets'],queryFn:api.targets});const [endpoint,setEndpoint]=useState<Endpoint>(()=>({host:'',port:104,called_ae:'',calling_ae:loadLocalSettings().callingAe}));const [filters,setFilters]=useState(()=>({date:localDateString(),modality:'',station_ae:'',patient_id:'',accession_number:'',patient_name:''}));const [result,setResult]=useState<Result>();const [selected,setSelected]=useState<WorklistEntry>();const mutation=useMutation({mutationFn:(broad:boolean)=>api.mwl({...endpoint,broad,filters}),onSuccess:setResult});const submit=(event:FormEvent)=>{event.preventDefault();mutation.mutate(false)};const retry=(key:'station_ae'|'modality')=>{const next={...filters,[key]:''};setFilters(next);api.mwl({...endpoint,broad:false,filters:next}).then(setResult)};const technical=useMemo(()=>JSON.stringify({association:{called_ae:endpoint.called_ae,calling_ae:endpoint.calling_ae,target:`${endpoint.host}:${endpoint.port}`},query:{broad:result?.broad??false,filters:result?.active_filters},response:{status:result?.status,count:result?.count,duration_ms:result?.duration_ms}},null,2),[endpoint,result]);const columns:Column<WorklistEntry>[]=[{key:'patient',header:'Patientenname',render:r=>r.patient_name||'—'},{key:'id',header:'Patient ID',className:'mono',render:r=>r.patient_id},{key:'birth',header:'Geburtsdatum',render:r=>r.birth_date||'—'},{key:'modality',header:'Modalität',render:r=>r.modality},{key:'time',header:'Geplante Zeit',className:'mono',render:r=>`${r.start_date} ${r.start_time}`},{key:'accession',header:'Accession',className:'mono',render:r=>r.accession_number}];return <><PageHeader title="Worklist" subtitle="DICOM Worklist testen und Ergebnisse analysieren" actions={<Link to="/pacs-store"><Button variant="outline" icon={<UploadCloud size={16}/>}>PACS Store testen</Button></Link>}/><div className="worklist-layout"><SectionCard title="Worklist Test" subtitle="Abfrageparameter konfigurieren und Worklist abrufen"><form onSubmit={submit}><TargetSelector value={endpoint} onChange={setEndpoint} targets={targets} service="mwl"/><div className="form-divider"/><div className="query-fields"><FormField label="Datum" htmlFor="filter-date"><DateInput id="filter-date" value={filters.date} onChange={e=>setFilters({...filters,date:e.target.value})}/></FormField><FormField label="Modalität" htmlFor="filter-modality"><Select id="filter-modality" value={filters.modality} onChange={e=>setFilters({...filters,modality:e.target.value})}><option value="">Beliebig</option>{['CT','MR','US','CR','DX','OT'].map(item=><option key={item}>{item}</option>)}</Select></FormField><FormField label="Station AE" htmlFor="filter-station"><TextInput id="filter-station" className="mono" value={filters.station_ae} placeholder="leer für beliebig" onChange={e=>setFilters({...filters,station_ae:e.target.value.toUpperCase()})}/></FormField><FormField label="Patient ID" htmlFor="filter-patient-id"><TextInput id="filter-patient-id" className="mono" value={filters.patient_id} placeholder="leer für beliebig" onChange={e=>setFilters({...filters,patient_id:e.target.value})}/></FormField><FormField label="Accession Number" htmlFor="filter-accession"><TextInput id="filter-accession" className="mono" value={filters.accession_number} placeholder="leer für beliebig" onChange={e=>setFilters({...filters,accession_number:e.target.value})}/></FormField><FormField label="Patientenname" htmlFor="filter-name"><TextInput id="filter-name" value={filters.patient_name} placeholder="leer für beliebig" onChange={e=>setFilters({...filters,patient_name:e.target.value})}/></FormField></div><div className="form-actions"><Button type="submit" loading={mutation.isPending} icon={<ListChecks size={17}/>}>Worklist abfragen</Button><Button type="button" variant="outline" disabled={mutation.isPending} icon={<Search size={17}/>} onClick={()=>mutation.mutate(true)}>Broad Query</Button></div></form></SectionCard><div className="worklist-result-stack"><SectionCard title="Ergebnis" action={result&&<StatusBadge tone={result.success?'success':'error'}>{result.success?'Erfolgreich':'Fehler'}</StatusBadge>}>{!result&&!mutation.isPending&&<div className="empty-state"><ListChecks size={32}/><strong>Noch keine Abfrage ausgeführt</strong><span>Konfiguration prüfen und Worklist abfragen.</span></div>}{mutation.isPending&&<div className="loading-state"><span className="loader"/>Association wird aufgebaut und C-FIND ausgeführt …</div>}{mutation.error&&<AlertBox tone="error" title="Request fehlgeschlagen">{mutation.error.message}</AlertBox>}{result&&<>{result.success?<div className="result-checks">{(result.steps??[]).map(step=><span key={step}><CheckCircle2 size={16}/>{step}</span>)}<span><CheckCircle2 size={16}/>Antwortzeit: {result.duration_ms} ms</span><span><CheckCircle2 size={16}/>{result.count??0} Worklist-Einträge gefunden</span></div>:<AlertBox tone="error" title={result.message??'DICOM-Test fehlgeschlagen'}><span className="mono">{result.code??result.status}</span></AlertBox>}{result.success&&result.count===0&&<div className="diagnosis-box"><h3>Keine Worklist-Einträge gefunden</h3><p>Die DICOM-Verbindung funktioniert. Mindestens ein aktiver Filter liefert keine Treffer.</p><div className="form-actions">{filters.station_ae&&<Button variant="outline" onClick={()=>retry('station_ae')}>Ohne Station AE testen</Button>}{filters.modality&&<Button variant="outline" onClick={()=>retry('modality')}>Ohne Modalität testen</Button>}<Button variant="ghost" onClick={()=>mutation.mutate(true)}>Broad Query</Button></div></div>}<div className="form-actions"><SaveTargetButton endpoint={endpoint} service="mwl"/></div></>}</SectionCard>{result?.entries&&result.entries.length>0&&<SectionCard title={`Worklist Ergebnisse (${result.entries.length})`}><DataTable columns={columns} rows={result.entries} getKey={(row,index)=>`${row.patient_id}-${index}`} onRowClick={setSelected}/><CollapsiblePanel title="Technische Details (DICOM)" action={<CopyButton value={technical}/>}><pre className="mono">{technical}</pre></CollapsiblePanel></SectionCard>}</div></div><Modal open={Boolean(selected)} title="Worklist Dataset" size="lg" onClose={()=>setSelected(undefined)}>{selected&&<><div className="result-summary result-summary--success"><strong>{selected.patient_name||'Unbekannter Patient'}</strong><div className="mono">{selected.patient_id} · {selected.accession_number}</div></div><DicomTree items={selected.dataset}/></>}</Modal></>}
