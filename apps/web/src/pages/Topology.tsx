import {FormEvent,useState} from 'react'
import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query'
import {Edit3,MapPin,Network,Plus,Server,Trash2} from 'lucide-react'
import {api,Area,DicomEndpoint,DicomSystem,Site} from '../api/client'
import {PageHeader} from '../components/layout/PageHeader'
import {Button} from '../components/ui/Button'
import {SectionCard} from '../components/ui/Card'
import {LoadingState} from '../components/ui/DataTable'
import {AlertBox} from '../components/ui/Feedback'
import {FormField,NumberInput,Select,TextInput} from '../components/ui/Form'
import {Modal} from '../components/ui/Overlay'

type Kind='site'|'area'|'system'|'endpoint'
type Dialog={kind:Kind;id?:number}
type EndpointDraft={system_id:number;name:string;service:DicomEndpoint['service'];host:string;port:number;called_ae:string}
const emptyEndpoint:EndpointDraft={system_id:0,name:'',service:'MWL',host:'',port:104,called_ae:''}

export function Topology(){
 const queryClient=useQueryClient()
 const sites=useQuery({queryKey:['sites'],queryFn:api.sites})
 const areas=useQuery({queryKey:['areas'],queryFn:api.areas})
 const systems=useQuery({queryKey:['dicom-systems'],queryFn:api.dicomSystems})
 const endpoints=useQuery({queryKey:['dicom-endpoints'],queryFn:api.dicomEndpoints})
 const [dialog,setDialog]=useState<Dialog>()
 const [name,setName]=useState('')
 const [siteId,setSiteId]=useState(0)
 const [endpoint,setEndpoint]=useState(emptyEndpoint)
 const invalidate=()=>Promise.all(['sites','areas','dicom-systems','dicom-endpoints','modality-profiles'].map(key=>queryClient.invalidateQueries({queryKey:[key]})))
 const close=()=>{setDialog(undefined);setName('');setSiteId(0);setEndpoint(emptyEndpoint)}
 const save=useMutation({mutationFn:async()=>{if(!dialog)throw new Error('Kein Dialog geöffnet');if(dialog.kind==='site')return dialog.id?api.updateSite(dialog.id,{name}):api.createSite({name});if(dialog.kind==='area')return dialog.id?api.updateArea(dialog.id,{name,site_id:siteId}):api.createArea({name,site_id:siteId});if(dialog.kind==='system')return dialog.id?api.updateDicomSystem(dialog.id,{name}):api.createDicomSystem({name});const {system_id,...value}=endpoint;return dialog.id?api.updateDicomEndpoint(dialog.id,value):api.createDicomEndpoint(system_id,value)},onSuccess:async()=>{await invalidate();close()}})
 const remove=useMutation({mutationFn:async({kind,id}:{kind:Kind;id:number})=>{if(kind==='site')return api.deleteSite(id);if(kind==='area')return api.deleteArea(id);if(kind==='system')return api.deleteDicomSystem(id);return api.deleteDicomEndpoint(id)},onSuccess:invalidate})
 const openNew=(kind:Kind,parentId?:number)=>{close();if(kind==='area')setSiteId(parentId??sites.data?.[0]?.id??0);if(kind==='endpoint')setEndpoint({...emptyEndpoint,system_id:parentId??systems.data?.[0]?.id??0});setDialog({kind})}
 const edit=(kind:Kind,item:Site|Area|DicomSystem|DicomEndpoint)=>{close();if(kind==='site'||kind==='system')setName(item.name);if(kind==='area'){const value=item as Area;setName(value.name);setSiteId(value.site_id)}if(kind==='endpoint'){const value=item as DicomEndpoint;setEndpoint({system_id:value.system_id,name:value.name,service:value.service,host:value.host,port:value.port,called_ae:value.called_ae})}setDialog({kind,id:item.id})}
 const destroy=(kind:Kind,id:number,label:string)=>{if(confirm(`„${label}“ löschen? Verknüpfte Profile bleiben erhalten und werden entkoppelt.`))remove.mutate({kind,id})}
 const submit=(event:FormEvent)=>{event.preventDefault();save.mutate()}
 const loading=[sites,areas,systems,endpoints].some(query=>query.isLoading)
 const failed=[sites,areas,systems,endpoints].find(query=>query.error)
 const dialogTitle={site:'Standort',area:'Bereich',system:'DICOM-System',endpoint:'Endpoint'}[dialog?.kind??'site']
 return <>
  <PageHeader title="Standorte und DICOM-Systeme" subtitle="Organisation und technische DICOM-Endpoints verwalten" actions={<div className="form-actions topology-actions"><Button icon={<MapPin size={16}/>} onClick={()=>openNew('site')}>Standort hinzufügen</Button><Button variant="outline" icon={<Server size={16}/>} onClick={()=>openNew('system')}>DICOM-System hinzufügen</Button></div>}/>
  {failed&&<AlertBox tone="error" title="Topologie konnte nicht geladen werden">{failed.error?.message}</AlertBox>}
  {loading?<LoadingState/>:!failed&&<div className="topology-layout"><SectionCard title="Standorte und Bereiche" subtitle="Organisatorische Zuordnung der Modalitäten">{(sites.data??[]).map(site=><section className="topology-group" key={site.id}><div className="topology-heading"><h2><MapPin size={18}/>{site.name}</h2><div className="table-actions"><Button variant="ghost" aria-label={`${site.name} bearbeiten`} icon={<Edit3 size={15}/>} onClick={()=>edit('site',site)}/><Button variant="ghost" aria-label={`${site.name} löschen`} icon={<Trash2 size={15}/>} onClick={()=>destroy('site',site.id,site.name)}/><Button variant="outline" icon={<Plus size={15}/>} onClick={()=>openNew('area',site.id)}>Bereich</Button></div></div>{(areas.data??[]).filter(area=>area.site_id===site.id).map(area=><div className="topology-node" key={area.id}><div><strong>{area.name}</strong><small>Modalitätsbereich</small></div><div className="table-actions"><Button variant="ghost" aria-label={`${area.name} bearbeiten`} icon={<Edit3 size={15}/>} onClick={()=>edit('area',area)}/><Button variant="ghost" aria-label={`${area.name} löschen`} icon={<Trash2 size={15}/>} onClick={()=>destroy('area',area.id,area.name)}/></div></div>)}{!(areas.data??[]).some(area=>area.site_id===site.id)&&<p className="muted">Noch keine Bereiche.</p>}</section>)}{(sites.data??[]).length===0&&<div className="empty-state"><MapPin size={30}/><strong>Noch keine Standorte</strong><span>Lege zuerst einen Standort an.</span></div>}</SectionCard><SectionCard title="Technische DICOM-Systeme" subtitle="Endpoints nach Dienst, Host, Port und Called AE">{(systems.data??[]).map(system=><section className="topology-group" key={system.id}><div className="topology-heading"><h2><Server size={18}/>{system.name}</h2><div className="table-actions"><Button variant="ghost" aria-label={`${system.name} bearbeiten`} icon={<Edit3 size={15}/>} onClick={()=>edit('system',system)}/><Button variant="ghost" aria-label={`${system.name} löschen`} icon={<Trash2 size={15}/>} onClick={()=>destroy('system',system.id,system.name)}/><Button variant="outline" icon={<Plus size={15}/>} onClick={()=>openNew('endpoint',system.id)}>Endpoint</Button></div></div>{(endpoints.data??[]).filter(item=>item.system_id===system.id).map(item=><div className="topology-node topology-endpoint" key={item.id}><Network size={17}/><div><strong>{item.name} · {item.service}</strong><small><span className="mono">{item.host}:{item.port}</span> · Called AE <span className="mono">{item.called_ae}</span></small></div><div className="table-actions"><Button variant="ghost" aria-label={`${item.name} bearbeiten`} icon={<Edit3 size={15}/>} onClick={()=>edit('endpoint',item)}/><Button variant="ghost" aria-label={`${item.name} löschen`} icon={<Trash2 size={15}/>} onClick={()=>destroy('endpoint',item.id,item.name)}/></div></div>)}{!(endpoints.data??[]).some(item=>item.system_id===system.id)&&<p className="muted">Noch keine Endpoints.</p>}</section>)}{(systems.data??[]).length===0&&<div className="empty-state"><Server size={30}/><strong>Noch keine DICOM-Systeme</strong><span>Lege ein System und seine technischen Endpoints an.</span></div>}</SectionCard></div>}
  <Modal open={Boolean(dialog)} title={`${dialog?.id?'Bearbeiten':'Neu'}: ${dialogTitle}`} size="lg" onClose={close}><form className="target-form" onSubmit={submit}>{(dialog?.kind==='site'||dialog?.kind==='system')&&<FormField label={dialog.kind==='site'?'Standortname':'Systemname'} htmlFor="node-name" className="span-all"><TextInput id="node-name" autoFocus required value={name} onChange={event=>setName(event.target.value)}/></FormField>}{dialog?.kind==='area'&&<><FormField label="Bereichsname" htmlFor="node-name"><TextInput id="node-name" autoFocus required value={name} onChange={event=>setName(event.target.value)}/></FormField><FormField label="Standort" htmlFor="area-site"><Select id="area-site" required value={siteId||''} onChange={event=>setSiteId(Number(event.target.value))}><option value="">Auswählen</option>{sites.data?.map(item=><option value={item.id} key={item.id}>{item.name}</option>)}</Select></FormField></>}{dialog?.kind==='endpoint'&&<><FormField label="DICOM-System" htmlFor="endpoint-system"><Select id="endpoint-system" required disabled={Boolean(dialog.id)} value={endpoint.system_id||''} onChange={event=>setEndpoint({...endpoint,system_id:Number(event.target.value)})}><option value="">Auswählen</option>{systems.data?.map(item=><option value={item.id} key={item.id}>{item.name}</option>)}</Select></FormField><FormField label="Endpointname" htmlFor="endpoint-name"><TextInput id="endpoint-name" required value={endpoint.name} onChange={event=>setEndpoint({...endpoint,name:event.target.value})}/></FormField><FormField label="Dienst" htmlFor="endpoint-service"><Select id="endpoint-service" value={endpoint.service} onChange={event=>setEndpoint({...endpoint,service:event.target.value as DicomEndpoint['service']})}>{['MWL','STORE','QR'].map(value=><option key={value}>{value}</option>)}</Select></FormField><FormField label="Host" htmlFor="endpoint-host"><TextInput id="endpoint-host" className="mono" required value={endpoint.host} onChange={event=>setEndpoint({...endpoint,host:event.target.value})}/></FormField><FormField label="Port" htmlFor="endpoint-port"><NumberInput id="endpoint-port" min={1} max={65535} required value={endpoint.port} onChange={event=>setEndpoint({...endpoint,port:Number(event.target.value)})}/></FormField><FormField label="Called AE" htmlFor="endpoint-ae"><TextInput id="endpoint-ae" className="mono" maxLength={16} required value={endpoint.called_ae} onChange={event=>setEndpoint({...endpoint,called_ae:event.target.value.toUpperCase()})}/></FormField></>}{save.error&&<div className="span-all"><AlertBox tone="error">{save.error.message}</AlertBox></div>}{remove.error&&<div className="span-all"><AlertBox tone="error">{remove.error.message}</AlertBox></div>}<div className="form-actions span-all"><Button type="button" variant="ghost" onClick={close}>Abbrechen</Button><Button type="submit" loading={save.isPending}>Speichern</Button></div></form></Modal>
 </>
}