import {useMemo,useState} from 'react'
import {useQuery} from '@tanstack/react-query'
import {ArrowLeft,Search} from 'lucide-react'
import {Link,useNavigate,useParams} from 'react-router-dom'
import {api,ModalityCheckResult,Run} from '../api/client'
import {DicomLogViewer} from '../components/dicom/DicomLogViewer'
import {ModalityCheckResultPanel} from '../components/dicom/ModalityCheckResult'
import {TestResultSummary} from '../components/dicom/TestResultSummary'
import {PageHeader} from '../components/layout/PageHeader'
import {Button} from '../components/ui/Button'
import {SectionCard} from '../components/ui/Card'
import {Column,DataTable,LoadingState} from '../components/ui/DataTable'
import {CollapsiblePanel} from '../components/ui/Disclosure'
import {FormField,Select,TextInput} from '../components/ui/Form'
import {KeyValueList} from '../components/ui/KeyValueList'
import {StatusBadge} from '../components/ui/Status'
const label=(type:string)=>type==='mwl_find'?'Worklist':type==='dicom_store'?'PACS Store':type==='modality_check'?'Modalitätsprüfung':'C-ECHO'
export function History(){const navigate=useNavigate();const query=useQuery({queryKey:['runs'],queryFn:api.runs});const [type,setType]=useState('');const [status,setStatus]=useState('');const [search,setSearch]=useState('');const data=useMemo(()=>(query.data??[]).filter(run=>(!type||run.test_type===type)&&(!status||String(run.success)===status)&&(!search||JSON.stringify(run).toLowerCase().includes(search.toLowerCase()))),[query.data,type,status,search]);const columns:Column<Run>[]=[{key:'time',header:'Zeit',render:r=>new Date(r.started_at).toLocaleString('de-DE')},{key:'type',header:'Typ',render:r=>label(r.test_type)},{key:'target',header:'Ziel / Profil',className:'mono',render:r=>r.test_type==='modality_check'?String(r.result_json.profile_name??'Modalitätsprofil'):r.manual_target_json?.host??`Ziel #${r.target_id}`},{key:'status',header:'Status',render:r=><StatusBadge tone={r.success?'success':'error'}>{r.success?'Erfolgreich':'Fehler'}</StatusBadge>},{key:'duration',header:'Dauer',render:r=>`${r.duration_ms} ms`},{key:'result',header:'Ergebnis',render:r=>r.test_type==='mwl_find'?`${r.result_json.count??0} Treffer`:r.status},{key:'details',header:'',render:()=> <span className="table-link">Details →</span>}];return <><PageHeader title="Test-Historie" subtitle="Durchgeführte DICOM-Tests filtern und technisch nachvollziehen"/><SectionCard><div className="history-filters"><FormField label="Typ" htmlFor="history-type"><Select id="history-type" value={type} onChange={e=>setType(e.target.value)}><option value="">Alle Typen</option><option value="mwl_find">Worklist</option><option value="dicom_store">PACS Store</option><option value="modality_check">Modalitätsprüfung</option><option value="dicom_echo">C-ECHO</option></Select></FormField><FormField label="Status" htmlFor="history-status"><Select id="history-status" value={status} onChange={e=>setStatus(e.target.value)}><option value="">Alle Status</option><option value="true">Erfolgreich</option><option value="false">Fehler</option></Select></FormField><FormField label="Suche" htmlFor="history-search" className="span-two"><div className="input-with-icon"><Search size={16}/><TextInput id="history-search" value={search} placeholder="Host, Profil, AE Title oder Status" onChange={e=>setSearch(e.target.value)}/></div></FormField></div>{query.isLoading?<LoadingState/>:<DataTable columns={columns} rows={data} getKey={row=>row.id} onRowClick={row=>navigate(`/history/${row.id}`)} empty="Keine Tests für diese Filter gefunden."/>}</SectionCard></>}
export function HistoryDetail(){const {id=''}=useParams();const query=useQuery({queryKey:['run',id],queryFn:()=>api.run(id)});if(query.isLoading)return <LoadingState/>;if(!query.data)return <SectionCard><p>Historieneintrag nicht gefunden.</p></SectionCard>;const run=query.data;const endpoint=run.manual_target_json;const header=<PageHeader title={`${label(run.test_type)} · Test #${run.id}`} subtitle={new Date(run.started_at).toLocaleString('de-DE')} breadcrumbs={['Test-Historie',`Test #${run.id}`]} actions={<Link to="/history"><Button variant="outline" icon={<ArrowLeft size={16}/>}>Zurück</Button></Link>}/>;if(run.test_type==='modality_check'){const result={...(run.result_json as ModalityCheckResult),run_id:run.id};return <>{header}<SectionCard title={result.profile_name}><ModalityCheckResultPanel result={result} showHistoryLink={false}/></SectionCard></>}return <>{header}<div className="dashboard-grid"><SectionCard title="Testergebnis"><TestResultSummary result={run.result_json}/></SectionCard><SectionCard title="Testparameter"><KeyValueList items={[{label:'Testtyp',value:label(run.test_type)},{label:'Host',value:endpoint?.host??`Ziel #${run.target_id}`,mono:true},{label:'Port',value:endpoint?.port,mono:true},{label:'Calling AE',value:endpoint?.calling_ae,mono:true},{label:'Called AE',value:endpoint?.called_ae,mono:true},{label:'Dauer',value:`${run.duration_ms} ms`}]} /></SectionCard></div><SectionCard title="Technisches Log"><DicomLogViewer result={run.result_json} context={{target:endpoint?`${endpoint.host}:${endpoint.port}`:undefined,calling:endpoint?.calling_ae,called:endpoint?.called_ae}}/><CollapsiblePanel title="Gespeicherte Request-/Response-Daten"><pre className="mono">{JSON.stringify(run.result_json,null,2)}</pre></CollapsiblePanel></SectionCard></>}
